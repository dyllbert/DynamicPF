using namespace std;

class RobotState {
    private:

    protected:
        
    public:
        RobotState();
        RobotState(double x, double y, double theta);
        double x;
        double y;
        double theta;
};